# demo-workspace

这是一个不含密钥的最小演示工作区。进入本项目根目录后运行：

```bash
.venv/bin/agent-code run "请使用 echo 工具回答：演示成功"
.venv/bin/agent-code repl
```

在 REPL 中尝试 `/status`、`/plan`、`/todo add 阅读架构说明`、`/skills` 和 `/help`。
不要把真实密钥放入这个目录；真实模型配置应只在当前终端环境中设置。
