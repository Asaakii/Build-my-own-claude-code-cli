# 学习日志

## 2026-06-12

### 阶段 0.1：项目环境与安全基线

- 完成 Claude Code 风格 CLI 的资料评估、范围界定与分阶段开发计划。
- 建立本地 Git 仓库、GitHub 远程关联、Python 3.12.13 虚拟环境和可安装项目骨架。
- 建立依赖、测试、静态检查、凭据模板与 Git 忽略规则；`.env`、虚拟环境和私有复盘不会进入版本控制。
- 验证结果：项目可编辑安装；`agent-code --help`、`agent-code version`、测试与 Ruff 检查均可执行。
- 关键理解：开发 Agent 功能前，先确保运行环境可重建、测试可重复执行，并使密钥和私有运行数据与版本控制隔离。

### 阶段 0.2：最小命令行与 Agent Loop

- 实现命令组、帮助页和版本命令，避免单命令模式导致子命令解析错误。
- 建立消息、工具调用和模型响应的数据模型，以及 Provider 协议与可控的 MockProvider。
- 实现 echo 工具和最小 Agent Loop，支持直接回答、工具结果回填、未知工具错误回传与最大轮数保护。
- 验证结果：Python 3.12.13 环境下共 10 项测试通过；`ruff check .` 通过；帮助页和版本命令输出符合预期。
- 关键理解：模型负责决定是否调用工具；Agent Loop 负责执行工具、把结果写回消息历史，并在模型给出最终文本或达到安全上限时结束。

### 阶段 0.3：一次性运行与交互式终端

- 实现本地 DemoProvider、`agent-code run` 与 `agent-code repl`，将命令行输入接入最小 Agent Loop。
- REPL 支持连续输入、空输入跳过、`/exit`、`/quit`、EOF 与 Ctrl+C 退出。
- 验证结果：Python 3.12.13 环境下共 14 项测试通过；`ruff check .` 通过；一次性运行和 REPL 均完成“请求 echo 工具 → 回填结果 → 输出最终文本”的本地端到端演示。
- 关键理解：CLI 只负责接收和呈现用户输入输出；Agent Loop 负责运行时循环；Provider 决定模型响应。三者分离后，后续替换真实模型不需要重写 CLI 或工具逻辑。

### 阶段 1.1：Anthropic 兼容协议适配

- 引入 Anthropic Python SDK，并将工具协议扩展为名称、说明和 JSON Schema。
- 实现 AnthropicProvider，将内部消息转换为 Messages API 的文本、`tool_use` 与 `tool_result` 内容块。
- 保留 MockProvider 与 DemoProvider，测试时使用假客户端，不发起真实网络请求或读取真实密钥。
- 验证结果：Python 3.12.13 环境下共 16 项测试通过；Ruff 自动修正 import 排序后复查通过。
- 关键理解：真实模型接入的关键不只是发送提示词，还要严格保持“assistant 发出 tool_use，user 回传 tool_result”的消息顺序；协议转换集中在 Provider，Agent Loop 不应感知具体 SDK。

### 阶段 1.2：Provider 配置与安全选择

- 实现 Anthropic 配置加载，支持从命令行选项或环境变量读取模型名、Base URL 与凭据；命令行选项优先。
- 为 `run` 与 `repl` 添加 `--provider`、`--model`、`--base-url`；默认仍使用本地 demo，不会意外调用真实模型。
- 未选择受支持 Provider，或选择真实 Provider 但缺少配置时，命令行给出明确错误并以状态码 2 退出。
- 验证结果：Python 3.12.13 环境下共 21 项测试通过；`ruff check .` 通过；默认 demo 正常运行，未配置真实 Provider 时未联网并提示缺少 `ANTHROPIC_API_KEY` 和 `AGENT_CODE_MODEL`。
- 关键理解：真实 Provider 必须是显式选择；配置校验应发生在网络调用之前，并且错误信息只指出缺失配置名称，绝不输出密钥内容。

### 阶段 1.3：模型服务错误处理

- 将 Anthropic SDK 的认证失败、限流、超时、连接与服务端异常转换为统一的 ProviderError。
- 一次性运行在模型服务失败时以明确状态码退出；REPL 显示错误后保持会话，不因一次失败崩溃。
- 禁用 SDK 的自动重试，避免在初版中因隐式重试增加难以观察的等待、额度消耗或重复行为。
- 验证结果：Python 3.12.13 环境下共 24 项测试通过；`ruff check .` 通过。测试覆盖认证、限流、连接错误的中文提示，并验证错误文本不包含测试密钥。
- 关键理解：网络与模型错误应在 Provider 边界被转换为稳定、可操作且不含敏感信息的应用错误；CLI 负责呈现和退出策略，而不应直接暴露 SDK 异常。

### 阶段 1.4：Provider 配置状态

- 实现 `agent-code status`，在不创建网络客户端的前提下检查本地 demo 与 Anthropic Provider 的配置状态。
- 状态命令仅显示“已配置”或缺失变量名称，不显示密钥、模型名或 Base URL。
- 验证结果：Python 3.12.13 环境下共 26 项测试通过；`ruff check .` 通过；未配置真实 Provider 时，`status` 正确报告 demo 可用并列出缺失的配置名称。
- 关键理解：配置状态检查必须和网络调用解耦；可观察性应帮助定位问题，但不能为了方便排查而输出敏感配置值。

### 阶段 1.5：DeepSeek 真实工具调用验证

- 使用 DeepSeek 的 Anthropic 兼容接口和 `deepseek-v4-flash` 模型进行本机冒烟验证；密钥仅存在于当前终端环境变量中。
- 先验证真实文本响应，再要求模型调用 `echo` 工具并原样返回工具结果。
- 验证结果：`agent-code status` 正确显示真实 Provider 已配置；真实调用返回“DeepSeek 连通成功”，工具调用最终返回“来自 DeepSeek 的工具调用”。此前的 26 项自动化测试与 `ruff check .` 均已通过。
- 关键理解：兼容 Provider 的正确性不能仅由 SDK 单元测试推断；必须用一次受控的真实调用验证“tool_use → 本地工具执行 → tool_result → 最终回答”的完整链路，同时避免把密钥写入仓库或日志。

### 阶段 3.1：安全文件读取

- 新增 `read_file` 工具，支持工作区内 UTF-8 文本读取、行号范围与带行号的输出。
- 规范化目标路径并限制在工作区根目录内，拒绝绝对路径、`..` 越界、符号链接绕行、目录、二进制文件、非 UTF-8 文件和超过 100 KiB 的文件。
- 为长文件输出设置 200 行、12,000 字符和单行 500 字符上限，并在截断时提示用行号范围继续读取。
- 验证结果：Python 3.12.13 环境下 30 项测试通过；`ruff check .` 通过；DeepSeek 真实调用成功读取 `src/agent_code/models.py` 并正确列出 `ToolCall`、`Message` 和 `ModelResponse`。
- 关键理解：读取文件同样属于安全边界。路径必须在解析符号链接后校验归属；输出也必须受限，否则只读工具仍可能造成上下文膨胀或暴露工作区外内容。

### 阶段 3.2：目录列举与路径 glob

- 新增 `list_dir` 工具，列举工作区内目录的直接子项；新增 `glob` 工具，按相对模式查找工作区内文件和目录。
- 两项工具都拒绝绝对路径、`..`，跳过符号链接，限制最多返回 200 项，并在截断时明确提示。
- 修正目录列举测试的错误前提：达到结果上限后工具不承诺继续扫描，因此将待验证的符号链接置于排序前部，以实际验证其被跳过。
- 验证结果：Python 3.12.13 环境下 36 项测试通过；`ruff check .` 通过；DeepSeek 真实调用依次列出工具目录，并通过 glob 找到 4 个 `test_*_tool.py` 测试文件后完成总结。
- 关键理解：受限列表工具的“最大返回数”应定义为扫描上限，而不是扫描完整目录后的筛选上限；否则大型目录会造成不受控的遍历成本。测试也必须与这一契约一致。

### 阶段 3.3：受限文本搜索

- 新增 `search_text` 工具，支持工作区内固定字符串搜索、限定搜索目录、可选大小写敏感和带路径、行号的匹配结果。
- 搜索跳过符号链接、二进制/非 UTF-8/超过 100 KiB 文件与常见缓存目录；最多检查 1000 个文件、返回 200 条结果，并限制总输出与单行长度。
- 验证结果：Python 3.12.13 环境下 40 项测试通过；`ruff check .` 通过；DeepSeek 在 `src/agent_code` 中搜索 `ProviderError`，得到 13 处匹配、涉及 4 个文件，结果均为工作区相对路径。
- 关键理解：文本搜索不应直接暴露正则表达式能力。固定字符串搜索足以覆盖首版代码定位需求，并可避免正则语义、回溯开销和超大输出带来的额外攻击面。

### 阶段 4.1：精确替换与 diff 预览

- `read_file` 现在输出内容 SHA-256；新增 `preview_replace`，要求调用者带回当前指纹后才能生成替换预览。
- 预览只允许非空 `old_text` 在目标文件中恰好出现一次，不允许模糊全文件覆盖；它拒绝过期指纹、目标不唯一、符号链接、工作区外路径、二进制/非 UTF-8/过大文件。
- 预览返回统一 diff 与替换后指纹，但绝不写入磁盘。
- 验证结果：Python 3.12.13 环境下 44 项测试通过；`ruff check .` 通过；DeepSeek 真实调用完成“读取 SHA-256 → 生成 `最小 echo 工具` 到 `示例 echo 工具` 的 diff”，并确认 `echo.py` 未写入。
- 关键理解：写入前的关键校验不是“模型声称它已经读过”，而是要求它提交针对当前文件版本的指纹；预览和写入都验证这一指纹，才能可靠发现预览后的外部修改。

### 阶段 4.2：显式确认后的受限写入

- 新增进程内待确认编辑仓库：预览生成一次性确认 ID；只有用户在 REPL 手动输入 `/approve <ID>` 才会触发写入，模型本身无法用工具参数绕过确认。
- 写入前再次校验文件 SHA-256 与唯一替换目标；写入使用同目录临时文件和原子替换，并在写入后复核 SHA-256。
- 验证结果：Python 3.12.13 环境下 47 项测试通过；`ruff check .` 通过。真实 REPL 预览后，经用户输入完整确认 ID，将 `echo.py` 的“最小 echo 工具”替换为“示例 echo 工具”，终端显示替换次数为 1 以及写入前后指纹。
- 关键理解：安全确认必须存在于模型控制面之外。一次性确认 ID 将“预览的具体内容”与“用户的明确批准”绑定；再配合写前指纹复查，外部并发修改不会被静默覆盖。

### 阶段 4.3：受限创建文件与最小审计摘要

- 新增 `preview_create_file`：仅允许在工作区内创建原本不存在的 UTF-8 文本文件；父目录必须已存在，路径越界、符号链接、二进制风险与超长内容都会被拒绝。
- 创建操作和精确替换共用一次性确认 ID；确认后采用同目录临时文件加原子创建，目标文件已出现时不会被覆盖。REPL 新增 `/audit`，仅显示时间、操作、结果、路径与 SHA-256，不记录文件内容。
- 验证结果：Python 3.12.13 环境下 50 项测试通过；`ruff check .` 自动修复后无剩余问题。
- 关键理解：创建文件比替换更容易误覆盖已有内容，因此“目标必须不存在”应在预览和最终写入时都验证；审计应支持追溯操作，而不能收集可能包含密钥或隐私的文件内容。

### 阶段 4.4：原子写入失败保全测试

- 通过测试注入 `os.replace` 与 `os.link` 的失败，分别模拟原子替换失败和原子创建失败；未修改生产代码。
- 替换失败时断言原文件内容保持不变；创建失败时断言目标文件不存在；两种情况都断言同目录临时文件已清理，审计记录为“已拒绝”。
- 验证结果：Python 3.12.13 环境下 52 项测试通过；`ruff check .` 通过。
- 关键理解：原子操作的安全目标不是“写完再回滚”，而是让失败发生时旧状态仍然完整；测试必须主动注入系统调用失败，才能验证异常路径的清理逻辑。

### 阶段 5.1：Shell 命令风险分类

- 新增 `check_command` 与 `CommandPolicy`：严格放行一小组只读命令；普通写入和未知命令标记为“需确认”；危险程序、网络访问、工作区外路径及管道/重定向等 Shell 组合语法直接拒绝。
- 此小节只返回权限判定，未实现 Shell 执行；因此模型无法通过该工具实际运行命令。
- 验证结果：Python 3.12.13 环境下 66 项测试通过；`ruff check .` 通过。测试覆盖只读、需确认、危险命令、网络访问、越界路径与额外参数拒绝。
- 关键理解：权限判断不能依赖模型的自我声明。先把命令解析和分类固定在程序边界，后续才可能在有限的规则下安全执行。

### 阶段 5.2：受限只读 Shell 执行

- 新增 `run_shell`：仅执行白名单内的 `pwd`、受限 `ls` / `rg` 和少量只读 Git 命令；使用参数数组调用子进程，不经过 Shell，工作目录固定为当前项目。
- 子进程不继承密钥等环境变量，只获得 PATH 与字符编码设置；执行超时为 5 秒，返回给模型的输出最多 12 KiB，并明确显示退出码。
- 写入或未知命令只返回“需确认、未执行”；删除、提权、网络、管道/重定向和工作区外路径被程序级拒绝。一次性确认后的写入执行尚未实现。
- 验证结果：Python 3.12.13 环境下 71 项测试通过；`ruff check .` 与 `git diff --check` 通过。
- 关键理解：安全执行的关键不是给模型一个终端，而是把命令解析、执行方式、工作目录、环境、时间与回传输出都收敛到可验证的边界中。

### 阶段 5.3：确认后受限命令执行

- 新增进程内待确认命令仓库与 REPL `/approve-command <ID>`；命令 ID 一次性有效，确认后会重新检查策略并写入最小审计摘要。
- 当前确认路径只允许 `touch` 与 `mkdir` 的工作区相对路径；确认不是任意程序的通行证，解释器等未知命令即使拿到 ID 仍会被拒绝，符号链接路径同样被拒绝。
- 审计只保留“Shell 命令”、状态和命令 SHA-256，不保存原始命令内容；编辑审计与命令审计通过同一 `/audit` 入口查看。
- 验证结果：Python 3.12.13 环境下 75 项测试通过；`ruff check .` 与 `git diff --check` 通过。
- 关键理解：确认必须绑定到预览中的精确命令，并在执行前重新裁定；用户确认可以提升有限能力，但不能扩大程序预先定义的安全边界。

### 阶段 6.1：脱敏 JSONL 会话仓库

- 新增工作区 `.agent-code/sessions/<会话 ID>.jsonl` 存储层：会话 ID 受格式校验，用户和助手消息可恢复；工具事件只保存受限摘要，不会作为模型历史恢复。
- 保存前会遮蔽常见 API Key、Token、Bearer 凭据和 `sk-` 形式令牌；单条对话限制为 8,000 个字符，工具摘要限制为 512 个字符，单会话文件限制为 1 MiB。
- 单个 JSONL 损坏行会被跳过并在会话列表中计数，不妨碍其余消息恢复；无效会话 ID 与达到容量上限的写入会被明确拒绝。
- 验证结果：Python 3.12.13 环境下 79 项测试通过；`ruff check .` 与 `git diff --check` 通过。
- 关键理解：可恢复的会话历史与工具执行记录用途不同。前者只保留 user / assistant 消息供后续对话注入，后者仅留脱敏摘要用于追溯，不能把完整工具输出默认永久化。

### 阶段 6.2：REPL 会话创建、列表与恢复

- `repl` 未传 `--session` 时创建新会话并显示会话 ID；传入 `--session <ID>` 时恢复既有 user / assistant 历史，不存在或格式无效的 ID 会以退出码 2 明确拒绝。
- 每个成功回合将用户输入与最终助手回答作为一组写入；写入失败只提示“会话未保存”，不会终止当前交互。`/sessions` 显示会话 ID、有效消息数和损坏行数。
- Agent 接受受限 `history` 参数；只有 user / assistant 消息能进入模型请求，工具结果不会因会话恢复而重新注入模型。
- 验证结果：Python 3.12.13 环境下 82 项测试通过；`ruff check .` 与 `git diff --check` 通过。
- 关键理解：持久化不是简单地把 REPL 输出追加到文件。恢复历史必须在模型调用前显式拼接，并且必须保持“历史消息”和“临时工具结果”两条不同的数据路径。

### 阶段 6.3：项目长期记忆与隐私边界

- 新增 `.agent-code/project-memory.json`：仅由 `/memory add <长期约定>` 显式写入，`/memory` 可审阅，`/memory remove <ID>` 可删除；文件采用临时文件加原子替换。
- 项目记忆只在每轮模型请求前临时注入，不进入 `AgentResult.messages` 或会话 JSONL；会话只保存 user / assistant，工具事件仍只保存受限摘要。
- 长期记忆限制为最多 50 条、单条 1,000 字符、文件 128 KiB、注入上下文 6,000 字符；疑似 API Key、Bearer 凭据、`sk-` 令牌和损坏记忆文件均拒绝写入或覆盖。
- 验证结果：Python 3.12.13 环境下 87 项测试通过；`ruff check .` 与 `git diff --check` 通过。
- 关键理解：记忆不是“更长的聊天记录”。它必须由用户显式管理、可删除且与短期会话和工具输出分层；临时注入模型不等于允许它被持久化为对话事实。

### 阶段 7.1：基础 Slash Commands

- REPL 新增 `/help`、`/clear`、`/session`、`/session list` 与 `/permissions`；`/sessions` 保留为兼容别名，既有 `/memory`、`/audit` 和确认命令继续可用。
- `/clear` 不会清空磁盘上的历史记录，而是新建空会话；会话列表和权限摘要不显示消息内容、原始命令或密钥。
- `/plan` 暂不提供空壳实现，将在阶段 8 与真正限制写入能力的 Plan Mode 一起接入。
- 验证结果：Python 3.12.13 环境下 88 项测试通过；`ruff check .` 与 `git diff --check` 通过。
- 关键理解：Slash Command 是 REPL 的控制面，必须与普通模型提示词分流；尤其是“清空”应有明确的数据语义，不能误导用户为删除了可恢复会话。

### 阶段 7.2：PreToolUse / PostToolUse Hooks

- 新增独立 Hook 协议：`PreToolUse` 可允许或拒绝某次工具调用，`PostToolUse` 只观察执行结果，不能回写或改变已完成操作。
- Agent 在调用工具前按顺序执行前置 Hook；拒绝结果会回填模型并通知后置 Hook。Hook 异常、错误返回类型与后置 Hook 异常均被隔离，不会中断会话。
- 后置 Hook 最多收到 512 个字符的工具结果摘要，避免 Hook 接口成为绕过工具输出限制的通道。
- 验证结果：Python 3.12.13 环境下 90 项测试通过；`ruff check .` 与 `git diff --check` 通过。
- 关键理解：Hook 是策略扩展点而非另一个 Agent Loop。它的拒绝能力必须发生在工具执行之前，而其自身失败必须被视为非关键故障并与主会话隔离。

### 阶段 7.3：受控声明式 Hook 配置

- Hook 只从项目根目录 `agent-code-hooks.json` 读取，格式固定为 version 1 的声明式 `deny_tools` 规则；不动态导入模块，不执行任意 Python 或 Shell 代码。
- 配置大小限制为 16 KiB；格式、版本或规则不支持时返回黄色警告并继续启动 REPL / 单次运行，默认不加载任何 Hook。
- 配置加载结果被传入 Agent；同一规则会在 REPL 和 `run` 中生效。
- 验证结果：Python 3.12.13 环境下 93 项测试通过；`ruff check .` 与 `git diff --check` 通过。
- 关键理解：项目可配置不等于项目代码可执行。将配置限制为小型声明式策略，才能既提供团队级约束，又不把 Hook 变成无边界的执行入口。

### 阶段 8.1：会话级 Plan Mode

- REPL 默认以 Plan Mode 启动；`/plan` 显示状态，`/plan on` 回到只读探索，用户显式输入 `/plan off` 后才在当前会话关闭。
- Plan Mode 作为最先执行的前置 Hook，拒绝 `preview_create_file`、`preview_replace` 与 `run_shell`；模型提示词不能绕过这层程序级限制。
- 关闭 Plan Mode 不会提升既有权限：文件写入仍需预览与确认，Shell 仍须经过权限引擎与一次性确认流程。
- 验证结果：Python 3.12.13 环境下 96 项测试通过；`ruff check .` 与 `git diff --check` 通过。
- 关键理解：Plan Mode 的关键不是让模型“承诺不写”，而是让写入能力在执行边界不可用；退出模式是显式用户动作，但也只能恢复已有权限。

### 阶段 8.2：持久化 Todo

- 新增独立 `.agent-code/todos.json`，支持 `/todo` 查看、`/todo add <内容>` 创建和 `/todo set <ID> <状态>` 更新。
- Todo 状态固定为 pending、in_progress、completed、blocked；存储采取大小上限、疑似凭据拒绝、损坏文件不覆盖和原子替换。
- 验证结果：Python 3.12.13 环境下 99 项测试通过；`ruff check .` 与 `git diff --check` 通过。
- 关键理解：Todo 是可持久化的任务状态，不是模型的自由文本。受限枚举与原子状态写入为后续任务图、崩溃恢复和并发领取提供了可靠基础。

### 阶段 8.3：结构化计划输出与批准

- `/plan` 现在输出 Plan Mode 状态和所有 Todo 步骤；`/plan add <步骤>` 可在只读计划期显式补充步骤，状态以 `(pending)` 等文本稳定呈现。
- 计划期保持写入/命令工具不可用；用户审阅步骤后输入 `/plan off` 才能仅在当前 REPL 会话恢复既有写入确认流程。
- 验证结果：Python 3.12.13 环境下 99 项测试通过；`ruff check .` 与 `git diff --check` 通过。
- 关键理解：计划输出必须是可审阅的结构化状态，而不是一句“我会做什么”的自然语言承诺；批准只改变会话模式，不授予新的工具权限。

### 阶段 9：Skills 按需加载

- 制定 `SKILL.md` 最小 front matter：name、description、applies_to；正文承载步骤和边界，提供 `skills/code-review/SKILL.md` 示例。
- `/skills` 仅扫描并显示元数据和来源；`/skill load <ID>` 才读取正文。技能不会写入项目记忆或会话 JSONL。
- 仅接受工作区 `skills/*/SKILL.md`，拒绝符号链接、越界/无效标识、非 UTF-8、格式缺失和超过 32 KiB 的技能；元数据读取最多 4 KiB。
- 验证结果：Python 3.12.13 环境下 102 项测试通过；`ruff check .` 与 `git diff --check` 通过。
- 关键理解：技能是受控的按需上下文，不是永久提示词。先发现能力摘要，再经显式选择加载正文，才能控制上下文占用与文件系统攻击面。

### 阶段 10：受限只读子代理

- 新增 `ReadOnlySubagentRunner`：每项研究创建独立 Agent、独立 Provider 与空工具集合；子代理不继承主会话历史，也不拥有写入或命令能力。
- 主代理只收到 task_id、终态和最多 2,000 字符结论；并发最多 2 项、每项最多 4 回合、递归深度最多 1。取消、失败、超时均转换为结构化结果。
- 超时后运行器不等待迟滞任务结束，避免阻塞主会话；并发测试验证两项独立任务不是串行等待。
- 验证结果：Python 3.12.13 环境下 106 项测试通过；`ruff check .` 与 `git diff --check` 通过。
- 关键理解：子代理是能力更小的独立会话，不是主代理的递归副本。主代理保留写入责任，并只消费受限结论而非子代理私有上下文。

### 阶段 11：上下文管理与成本可观测性

- 新增可替换 TokenEstimator 和 ContextReport，统计输入、模型输出与工具结果的字符/token 近似值；报告只保存聚合数字、截断次数、摘要消息数与触发原因，不保存原文或密钥。
- 工具输出进入模型消息前最多保留 2,000 字符；传入历史超过 8,000 字符时压缩较早消息、保留最近 4 条，并把来源和触发预算写入摘要文本。
- 未实现无测量缓存；测试验证长输出截断、替换估算器、摘要来源和压缩后工具任务仍能完成。
- 验证结果：Python 3.12.13 环境下 110 项测试通过；`ruff check .` 与 `git diff --check` 通过。
- 关键理解：上下文优化必须先可观测，再压缩；任何“忘记”都需要有明确来源和触发原因，不能以不可解释缓存替代数据边界。

### 阶段 12：任务协调与受限并发

- 新增独立任务图 `.agent-code/task-graph.json`：节点包含依赖、只读标记、状态、领取者和受限结果摘要；通过文件锁和原子替换实现领取、结束和恢复。
- 任务依赖未完成时不会变为可领取；只读且无依赖的节点最多两个并行交给子代理，写任务保留 pending，由主代理串行处理，不存在并发写入路径。
- `/task` 支持查看、添加只读/写任务、添加单依赖任务、恢复遗留领取和分派；重启检测到 in_progress 节点会恢复为 pending 并留下恢复说明。
- 验证结果：Python 3.12.13 环境下 113 项测试通过；`ruff check .` 与 `git diff --check` 通过。
- 关键理解：并发的安全前提是原子领取和明确能力分层。子代理只能领取就绪只读任务；写任务即便出现在任务图中，也不能被并发调度器取得。

### 阶段 13：Worktree 隔离与端到端演示

- 新增仅在 Git 仓库根目录可用的 Worktree 管理器：`/worktree create <任务ID>` 创建独立分支，`inspect` 显示状态、diff、固定测试结果和合并建议；非 Git 目录明确降级。
- 合并必须 `/worktree merge <ID> --confirm`；删除必须 `--confirm`。若 Worktree 有未提交改动，普通删除会失败以保留恢复路径，只有额外 `--discard-changes` 才允许丢弃改动。
- 临时 Git 样例端到端验证：隔离目录中修改 README 后，主工作区在合并前仍保持原内容；检查、清理失败和显式强制清理都有覆盖。
- 验证结果：Python 3.12.13 环境下 116 项测试通过；`ruff check .` 与 `git diff --check` 通过。
- 关键理解：Worktree 是隔离手段，不是自动合并器。创建隔离、审阅 diff、验证测试、明确合并和清理是分开的用户控制点，任何一步都不应隐式提交或推送。

### 阶段 14：MCP 与按需工具发现

- 实现最小本地 stdio MCP Client：使用逐行 JSON-RPC 完成初始化、工具分页发现、schema 校验、工具调用与断连。
- 启动阶段只暴露 `服务器.工具` 的名称与说明；只有搜索或调用匹配工具时才重新加载其输入 schema，避免把大量外部定义持续放入上下文。
- 所有 MCP 工具默认需要显式确认；Agent 侧工具门面只呈现命名空间和确认要求，不能绕过确认直接调用。单次文本输出最多保留 8,000 字符。
- 第一版刻意限定为本地 stdio，不配置 OAuth、云端连接器或组织级策略；这些能力必须在独立的认证、租户隔离与审计设计后再加入。
- 验证结果：Python 3.12.13 环境下 119 项测试通过；`ruff check .` 与 `git diff --check` 通过。测试覆盖本地 JSON-RPC 子进程握手、发现、调用、超时、schema 拒绝、确认边界和超大输出截断。
- 关键理解：MCP 解决的是工具互操作，不会天然授予可信执行权限。协议连接、工具发现和真正执行必须分层，外部服务返回的 schema 和输出同样需要受限处理。

### 阶段 15：打磨、发布与复盘

- 重写 README，明确这是学习项目而非官方 CLI；补充 Python 3.12.13 安装、无密钥 Mock 演示、真实模型配置边界、权限模型、REPL 命令、已知限制与故障排查。
- 新增架构图、安全回归清单、无密钥样例工作区和 `python -m agent_code` 入口；端到端测试在新工作区启动模块入口并完成 Mock 演示。
- REPL 增加 `/status`，仅汇总 Provider、会话消息数、Plan Mode 与 Todo 状态，不回显消息正文；同时修复 `/todo add` 含空格内容会被错误拒绝的问题。
- 验证结果：Python 3.12.13 环境下 121 项测试通过；`ruff check .` 与 `git diff --check` 通过。wheel 在全新 Python 3.12 虚拟环境中安装后，`agent-code --help` 与无密钥 Mock 演示成功，随后卸载验证通过。
- 真实模型验收随后使用 HermesLite 已有 DeepSeek 配置的临时变量映射完成；未复制密钥到本项目，也未写入公开日志。
- 关键理解：发布验收不只是“测试全绿”。安装路径、默认无密钥体验、公开文档的安全边界和未实现能力都必须可验证、可复现、可诚实说明。

### 阶段 15.1：受控真实 Provider 冒烟入口

- 新增 `agent-code smoke`：仅接受真实的 `anthropic` Provider，只暴露 `echo` 工具，并要求模型完成 `tool_use → echo(smoke-ok) → SMOKE_OK` 的完整回合。
- 验收命令不会提供文件或 Shell 工具，避免为一次连通性检查扩大副作用范围；若模型不调用工具或最终文本不精确匹配，命令以失败退出。
- 验证结果：命令的 Provider 限制、工具回合与最终结果由不联网协议替身测试覆盖；另以 HermesLite 的 `LLM_API_KEY`、`LLM_MODEL` 和 DeepSeek Anthropic 兼容地址临时映射运行，真实服务返回 `SMOKE_OK`，并观察到本地 `echo(smoke-ok)` 工具结果。
- 关键理解：真实冒烟应该是明确、可重复且能力最小的验收流程，而不是拿完整 Agent 在工作区上做不可预测的试运行。

### 阶段 16：受限 Telegram 私聊渠道

- 将 HermesLite 的 DeepSeek 模型信息和 Telegram 白名单配置迁移到当前项目的本地 `.env`；配置文件权限为 600，且被 Git 忽略。迁移过程只提取所需字段，不复制 HermesLite 的其他运行配置。
- 新增 `agent-code telegram status` 与 `agent-code telegram run`。标准库 HTTP 客户端仅调用 Telegram 的 `getMe`、`getUpdates` 和 `sendMessage`；offset 持久化在 `.agent-code/`，重启不会重复消耗旧消息。
- Telegram 只处理配置用户的私聊文本，按用户绑定会话；外部服务强制 Plan Mode，文件写入和 Shell 操作不能通过聊天绕过本地确认。
- 验证结果：Bot 身份接口成功返回 `@Asaakii_OpenClaw_bot`；Python 3.12.13 下完整自动化测试、Ruff 和差异检查通过。Bot Token、用户 ID、模型 Key 均未进入 Git 或公开学习日志。
- 关键理解：外部消息渠道不应等价于远程终端。白名单、私聊限制、offset 去重、会话隔离和程序级只读策略必须共同存在。

### 阶段 16.1：Telegram 身份事实与常驻修复

- 修复 Telegram Bot 把 Anthropic 兼容协议误称为自身模型身份的问题：`/start`、`/help` 与常见“你是什么模型”问题由程序明确回答“用户开发的 agent-code，底层使用 DeepSeek，不是 Claude”。普通消息也会带入不可覆盖的身份事实上下文。
- 长轮询的临时网络失败改为等待 5 秒后重试，避免一次 API 波动退出常驻服务；macOS 对 Downloads 后台访问受限时，运行时副本部署到用户 Application Support 目录并由 LaunchAgent 保持在线。
- 验证结果：Python 3.12.13 环境下 128 项测试、Ruff 和差异检查通过；更新后的 Bot 进程已由 LaunchAgent 重新启动。
- 关键理解：模型回答中的协议名称不是可靠的身份事实。产品身份、模型提供方、协议适配与项目作者必须由应用层明确区分，而不是让模型根据训练语料猜测。
