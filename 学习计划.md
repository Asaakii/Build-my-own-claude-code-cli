# 从零开发 Claude Code 风格 CLI：学习与实施计划

> 项目代号：`agent-code`  
> 当前状态：已完成资料评估与计划制定；尚未开始编码。  
> 定位：这是一个用于学习 Agent Harness 的、可运行且可测试的教学版命令行代码 Agent，不是 Claude Code 的产品级复刻。

## 1. 先校正目标与边界

### 已确认的事实

- 官方 GitHub 仓库 [`anthropics/claude-code`](https://github.com/anthropics/claude-code) 公开的是安装说明、问题反馈入口和插件示例；它**不是**完整的 Claude Code 源码仓库。[官方仓库](https://github.com/anthropics/claude-code)
- Claude Code 官方文档确认其 CLI 能力包括工具调用、权限、会话/记忆、技能、Hooks、子代理、MCP 等；这些是本项目可参考的公开行为边界，而不是要求一次性复刻的清单。[官方概览](https://code.claude.com/docs/en/overview)
- 本项目说明要求：核心代码由学习者优先手写；每一步更新本计划、`learning-log.md` 与个人复盘；不记录密钥或私有会话内容。

### 合理的项目约束

- 第一版只支持本地终端与单一工作区，不做 IDE、云端会话、账号登录、多平台安装器或官方同等性能优化。
- 第一版先兼容 Anthropic Messages API 的 `tool_use` / `tool_result` 消息形状，并通过 Provider 抽象兼容同协议服务；密钥仅从环境变量读取，绝不写入仓库或日志。
- 高风险能力分阶段开放：先只读，再经预览与确认写入，最后才执行命令、并行子代理、worktree 与 MCP。
- 计划中的“第 N 天”表示一个可独立完成和验证的学习单元，不承诺连续自然日完成。

### 明确不做

- 不复制、反编译或声称重建 Anthropic 的非公开实现。
- 不实现绕过用户确认的危险执行模式，不默认访问工作区外文件，不把 API Key、Token 或完整提示词写入持久化文件。
- 不在基础版本引入浏览器自动化、远程 SaaS 连接器、复杂调度平台或生产级多租户服务。

## 2. 资料评估与选用结论

| 资料 | 可信定位 | 参考价值 | 使用方式与限制 | 结论 |
| --- | --- | --- | --- | --- |
| [Anthropic 官方文档](https://code.claude.com/docs/en/overview) 与 [官方 GitHub 仓库](https://github.com/anthropics/claude-code) | 官方公开产品资料；不是完整源码 | 高：功能、术语、权限与配置的行为基准 | 用于核对能力边界、权限设计和公开接口；不从中推断内部算法或源码结构 | **权威规范来源** |
| [bozhouDev/14days-build-claude-code-cli](https://github.com/bozhouDev/14days-build-claude-code-cli) | Python 教学项目，明确声明为非完整复刻 | 很高：CLI 到 Agent Loop、工具、编辑、权限、会话、Hooks、Skills 的递进路线贴合本项目 | 作为主要手写主线。仓库说明称当前已有前 9 天教程与参考快照，后续章节不能假定已提供完成代码 | **主教材** |
| [shareAI-lab/learn-claude-code](https://github.com/shareAI-lab/learn-claude-code) | 17 章 Harness 教程，每章有独立 Python 示例 | 高：补充权限、上下文压缩、任务、后台、团队、MCP 与综合运行时 | 按机制选读和重写；各章多为独立示例，不能机械拼接成同一产品 | **核心补充教材** |
| [Claude Code from Source](https://claude-code-from-source.com/) | 基于公开 source map 的第三方逆向分析书；不是官方文档 | 中高：适合后期理解异步循环、工具并发、压缩、缓存等架构取舍 | 只用作“架构假设与设计灵感”，需与官方文档和可观测行为交叉核对；不将其中结论当作官方事实 | **后期选读/复核** |

### 采用顺序

1. 用官方文档确定公开行为和安全原则。
2. 按 14 天教程的学习顺序实现最小 CLI；每一步都自己写、自己运行。
3. 遇到主教材覆盖不足的机制，查 `learn-claude-code` 对应章节，提炼成适合本项目的最小实现。
4. 只有核心能力稳定后，阅读 *Claude Code from Source* 的相应章节，判断是否值得采用其性能或架构优化；没有测量数据时不提前优化。

## 3. 技术路线与目标架构

### 建议技术栈

- Python 3.12.13、`pip` + `.venv`：当前阶段的依赖与虚拟环境管理；后续可按需要迁移到 `uv`。
- `Typer`：命令行入口；`Rich`：可读的终端输出与确认提示。
- `pytest`：单元、集成与端到端测试；`ruff`：静态检查与格式检查。
- Anthropic Python SDK：真实 Provider；`MockProvider`：前期脱离网络和 API Key 的稳定测试。
- 标准库优先处理文件、子进程、JSONL、线程与时间；MCP 阶段再增加最小客户端依赖。

### 分层原则

```text
CLI / REPL / Slash 命令
           │
       Agent Loop ───────── 会话、上下文、待办
           │
  Provider 抽象（Mock / Anthropic-compatible）
           │
 工具注册表 ─ 权限引擎 ─ Hooks
   │         │
文件/搜索/编辑/命令/技能/子代理/MCP
           │
         工作区边界
```

- Agent Loop 只负责“请求模型 → 执行工具 → 回填结果 → 判断结束”，不把每个工具的细节塞进循环。
- 每个工具具备输入 schema、说明、执行器、可测试的返回值和明确风险等级。
- 权限由程序强制执行，不能只依靠系统提示词约束模型；该原则与 [官方权限文档](https://code.claude.com/docs/en/permissions) 一致。
- 任何持久化数据均落在项目内部专用目录；会话采用 JSONL，记忆与配置采用可审阅文本或 JSON。

## 4. 分阶段实施清单

每个单元完成的最低门槛：通过语法/静态检查、通过自动化测试、由学习者至少完成一次对应的真实运行验证。核心实现由助手按已确认范围直接提供；学习者负责粘贴、运行与反馈，助手负责说明目标、原因、检查结果和复盘。

### 阶段 0：项目骨架与安全基线（已完成）

- [x] 评估资料并确定主线、补充与权威来源。
- [x] 建立本计划、公开学习日志和私有复盘的文档约定。
- [x] 初始化 Python 项目、`src/` 布局和测试目录。
- [x] 建立 `.env.example`，配置 `.gitignore`，确保 `.env`、运行会话、缓存与个人复盘不进入版本控制。
- [x] 加入最小的 `--help`、版本输出、`pytest` 与 `ruff` 检查。

验收：空项目在没有 API Key 的情况下也能安装依赖、显示帮助并通过空测试集。

### 阶段 1（学习单元 1）：最小 CLI、REPL 与 Agent Loop（已完成）

- [x] 定义 `Message`、`ToolCall`、`Provider` 等最小领域模型。
- [x] 实现一次性提示词命令与交互式 REPL，支持退出和 Ctrl+C。
- [x] 编写不联网的 `MockProvider` 与 `echo` 工具。
- [x] 实现最小循环：模型响应文本则结束；请求工具则执行、追加结果并继续。

验收：Mock 场景覆盖“直接回答”“一次工具调用”“连续工具调用”“未知工具”“用户中断”。

### 阶段 2（学习单元 2）：真实模型与协议适配（已完成）

- [x] 实现 `AnthropicProvider`，将内部消息转换为 Messages API 请求与响应。
- [x] 从环境变量或命令行选项读取模型名、Base URL 与凭据；不回显真实值。
- [x] 增加“已配置/未配置”的状态展示；状态展示不得回显真实值。
- [x] 统一处理 API 超时、网络错误、限流、无效工具参数与模型异常结束。
- [x] 保留 MockProvider 作为全部测试的默认 Provider，真实调用只做手动冒烟验证。

验收：真实 API（由学习者自行配置）可调用 `echo`；没有密钥时提示可操作的配置说明且不崩溃。

### 阶段 3（学习单元 3）：只读工作区工具（已完成）

- [x] 实现安全文件读取。
- [x] 实现目录列举和路径 glob。
- [x] 实现文本搜索。
- [x] 用规范化路径与工作区根目录校验阻止 `..`、符号链接绕行和工作区外读取。
- [x] 为大文件、二进制文件、超长文件输出制定截断与摘要策略。
- [x] 在只读工具说明中清晰标示返回内容、限制和失败原因。

验收：可让 Agent 定位并解释工作区代码；工作区外路径、二进制文件、过大输出均被安全处理。

### 阶段 4（学习单元 4）：安全编辑能力

- [x] 实现读后快照校验与精确字符串替换预览；不做模糊全文件覆盖。
- [x] 生成统一 diff 预览，并在写入前请求用户通过 REPL 明确确认。
- [x] 实现受限精确替换写入与原子写入策略。
- [x] 实现受限创建文件与不含私密内容的审计摘要。
- [x] 为目标文本不唯一、文件已变化、越界和符号链接输出明确错误。
- [x] 为实际写入时的文件变化、确认 ID 失效、越界和符号链接输出明确错误。
- [x] 为原子替换与创建失败补充可注入的失败保全测试。

验收：用户可批准/拒绝 diff；拒绝后文件不变；测试覆盖替换成功、冲突、越界、拒绝，以及原子写入失败时的文件保全和临时文件清理。

### 阶段 5（学习单元 5）：Shell 工具与权限引擎

- [x] 建立风险分类：只读命令、普通写入命令、危险/拒绝命令。
- [x] 实现 Allow / Ask / Deny 规则、工作区 cwd、超时、输出上限与退出码回传。
- [x] 默认只自动执行一小组可验证的只读命令；写入或未知命令均标记为需确认且不会执行。
- [x] 对递归删除、工作区外路径、提权、网络下载、管道/重定向等高风险模式采用保守拒绝。
- [x] 将需确认命令绑定到 REPL 一次性确认 ID，并在确认后以受限方式执行与审计。

验收：规则优先级和确认流程有单测；危险命令不会因提示词而绕过程序级限制。

### 阶段 6（学习单元 6）：会话与项目记忆

- [x] 基础层：建立工作区内的脱敏 JSONL 会话仓库，限制会话 ID、单条内容和文件总大小，并可跳过损坏记录。
- [x] 将对话与工具事件以脱敏 JSONL 保存，支持列出、恢复和新建会话。
- [x] 实现项目记忆：显式用户指令、可审阅的长期约定、可删除条目。
- [x] 区分会话历史、项目记忆和临时工具输出，避免把所有内容永久保存。
- [x] 加入大小上限、损坏文件恢复与隐私提示。

验收：重启后能恢复指定会话；记忆能注入新会话；敏感环境变量和原始密钥不落盘。

### 阶段 7（学习单元 7）：Slash Commands、Hooks 与可控循环

- [ ] 实现 `/help`、`/clear`、`/session`、`/memory`、`/permissions`、`/plan` 等最小命令集。
- [x] 设计 `PreToolUse` / `PostToolUse` Hook 协议，Hooks 与主循环解耦。
- [x] Hook 配置仅从项目受控文件加载，记录失败但不让非关键 Hook 破坏会话。
- [ ] 如加入 `/loop`，必须有次数/时间上限和显式停止机制，不能默认为无限循环。

验收：命令路由、Hook 顺序、Hook 拒绝工具与异常隔离均有测试。

### 阶段 8（学习单元 8）：交互式 Shell、待办与 Plan Mode

- [ ] 优化 REPL：历史、可取消输入、状态展示、工具调用与确认提示的可读呈现。
- [ ] 实现磁盘持久化的 Todo 项，状态至少包含 pending / in_progress / completed / blocked。
- [ ] 实现 Plan Mode：只读探索、输出计划、用户批准后才允许编辑/命令写入。
- [ ] 确保“计划”不是安全绕过：工具权限仍由权限引擎裁定。

验收：在 Plan Mode 下写工具无法执行；批准后仅在本会话切换到允许的权限模式。

### 阶段 9（学习单元 9）：Skills 按需加载

- [ ] 制定 `SKILL.md` 的最小格式（名称、描述、适用条件、步骤、边界）。
- [ ] 启动时只读取技能元数据；模型或用户选择技能时才读取正文。
- [ ] 校验技能路径位于允许目录，限制尺寸并显示加载来源。
- [ ] 提供一个“代码审查”或“测试修复”示例技能，不将技能文本混入永久记忆。

验收：技能目录扫描、按需加载、缺失文件与恶意路径均有测试。

### 阶段 10（学习单元 10）：子代理

- [ ] 把子代理建模为受限的独立 Agent 会话，拥有独立消息历史、工具预算和任务说明。
- [ ] 初版只允许只读研究任务，主代理收到的是结构化结论而非完整私有上下文。
- [ ] 实现超时、取消、错误回传、最大递归深度与并发数上限。
- [ ] 明确主代理对写入操作的最终责任；子代理不直接绕过权限。

验收：子代理可并行检索两个只读子任务；取消和失败不会阻塞主会话。

### 阶段 11（学习单元 11）：上下文管理与成本可观测性

- [ ] 统计输入、输出与工具结果的近似 token/字符预算，并以可替换估算器实现。
- [ ] 依次采用工具输出截断、旧结果压缩、历史摘要三层策略。
- [ ] 保存摘要来源和触发原因，保证用户可理解“为什么忘掉了什么”。
- [ ] 不在没有测量的情况下实现复杂缓存；先以测试证明压缩后仍能完成关键任务。

验收：长工具输出不会无限增长；压缩前后的关键任务可回归测试；成本数据不含密钥。

### 阶段 12（学习单元 12）：任务协调与受限并发

- [ ] 将 Todo 扩展为带依赖的任务图，支持原子领取、状态转换与结果摘要。
- [ ] 主代理根据无依赖的只读任务分派给有限数量的子代理。
- [ ] 写任务仍由主代理串行执行或明确暂停确认，避免文件竞态。
- [ ] 为状态持久化、崩溃恢复与重复领取加入测试。

验收：两个独立研究任务可并行，存在依赖的任务不会提前运行，写任务不发生并发覆盖。

### 阶段 13（学习单元 13）：Worktree 隔离与端到端演示

- [ ] 仅在 Git 仓库中提供可选 worktree 工具；无 Git 时明确降级，不伪造隔离。
- [ ] 为每个可写子任务创建受控 worktree，完成后显示 diff、测试结果和合并建议。
- [ ] 合并、删除 worktree、Git 提交均必须由用户明确确认；默认不自动提交或推送。
- [ ] 编排端到端演示：读代码 → 制定计划 → 编辑 → 测试 → diff 审核。

验收：演示在样例仓库完成且主工作区在合并前保持不变；清理失败可恢复。

### 阶段 14（学习单元 14）：MCP 与工具发现

- [ ] 实现最小 stdio MCP 客户端：连接、工具列表、schema 校验、调用和断连。
- [ ] 所有外部 MCP 工具默认纳入权限引擎，带服务器名空间和输出大小限制。
- [ ] 工具较多时实现最小 ToolSearch：先载入服务摘要，按需检索并载入工具 schema。
- [ ] 不在第一版处理 OAuth、云端连接器或组织级策略；这些属于后续扩展。

验收：连接一个本地只读 MCP 示例服务器并成功调用；MCP 失败、超时、超大输出和需确认工具都有覆盖。

### 阶段 15：打磨、发布与复盘

- [ ] 补全 `README.md`：安装、配置、权限模型、示例、已知限制和故障排查。
- [ ] 补全架构图、CLI 帮助、示例技能、样例项目和端到端测试。
- [ ] 运行格式检查、完整测试、真实模型冒烟测试与安全回归清单。
- [ ] 打包为可安装 CLI；在干净环境验证安装、帮助、Mock 演示和卸载。
- [ ] 复盘每一阶段的设计取舍，列出未实现的官方能力与下一版优先级。

验收：新学习者按 README 可在无密钥模式运行 Mock 演示；完整测试通过；所有公开文档不含敏感信息。

## 5. 推荐的项目结构（随着阶段逐步出现）

```text
agent-code/
  src/agent_code/
    cli.py                 # CLI / REPL / slash commands
    agent.py               # Agent Loop
    providers/             # Mock 与真实模型 Provider
    tools/                 # 工具协议、文件、编辑、命令、MCP
    permissions/           # 规则与确认流程
    runtime/               # 会话、记忆、待办、上下文、Hooks
    subagents/             # 子代理与协调
  tests/
    unit/
    integration/
    e2e/
  examples/
  skills/
  .agent-code/             # 本地运行数据（默认忽略）
  pyproject.toml
  README.md
  学习计划.md
  learning-log.md
  实际指导过程复盘.md       # 私有文件，必须忽略
```

## 6. 每一步的协作与记录规则

1. 开始一个学习单元前，先把本计划中的目标项标为“进行中”，明确本次只做的最小范围。
2. 核心代码由助手按最小可验证范围直接提供；学习者粘贴、运行并反馈结果。助手同时说明代码放置位置、验证方式与关键设计原因。
3. 完成后执行静态检查、自动化测试和一次与本单元直接相关的真实运行验证。
4. 验证成功后，将本计划勾选为完成，在 `learning-log.md` 记录简洁结论，在 `实际指导过程复盘.md` 记录实际指导、设计理由、验证与调整。
5. 若测试、资料或运行结果推翻了当前假设，直接修改计划和结论；不为了保持原计划而跳过风险。
6. 每完成一个阶段小节并通过验收后，助手应提醒学习者提交并推送，提供一条简洁、准确的中文 Git 提交信息；除非学习者明确授权，提交与推送均由学习者执行。

## 7. 首次实施建议

下一次只启动“阶段 0 的代码骨架”，不要急于调用真实模型。先让 `agent-code --help`、MockProvider、测试与静态检查全部稳定；这样后续每个能力的失败都会更容易定位，也不会消耗 API 额度。
